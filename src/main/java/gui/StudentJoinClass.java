/*package gui;

import BusinessLogic.EnrolledStudents;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.TextField;

public class StudentJoinClass
{
    @FXML
    private TextField ClassroomCode;

    @FXML
    public void Submit(ActionEvent e){
        HelloController.BL.classrooms.Enrollstudent(ClassroomCode.getText(), (EnrolledStudents) HelloController.BL.students.GetloggedInStudent());
    }

    public void Back(ActionEvent e){
        HelloApplication.s.setWidth(310);
        HelloApplication.s.setHeight(390);
        HelloApplication.s.setScene(HelloController.studentmenu);
    }
}*/
